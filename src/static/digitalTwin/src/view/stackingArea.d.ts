import { Equipment } from './equipment';
/**
 * A stacking area class, which extends Equipment.
 */
export declare class StackingArea extends Equipment {
    constructor(posX?: number, posY?: number, labelText?: string);
    returnInstanceOf(posX: number, posY: number): StackingArea;
}
//# sourceMappingURL=stackingArea.d.ts.map