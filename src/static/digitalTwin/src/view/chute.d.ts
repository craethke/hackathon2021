import { Equipment } from './equipment';
/**
 * A chute class, which extends Equipment.
 */
export declare class Chute extends Equipment {
    constructor(posX?: number, posY?: number, labelText?: string, width?: number, height?: number);
    returnInstanceOf(posX: number, posY: number): Chute;
}
//# sourceMappingURL=chute.d.ts.map